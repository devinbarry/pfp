allow-branch = ["master"]
sign-commit = false
sign-tag = false
push = false
tag-name = "v{{version}}"
pre-release-commit-message = "chore: release v{{version}}"
# Changelog is maintained manually; git-cliff draft is generated by `just release-prep`.
# pre-release-hook = ["git-cliff", "-o", "CHANGELOG.md", "--tag", "{{version}}"]
